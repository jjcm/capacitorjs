#wow-container
  script(src="/pages/home.js")
  .header(load-order="primary")
    h1 Wow that was so fast!
    p Feel free to click the back button - Capacitor takes advantage of javascript history states, so you can navigate naturally.

  .loading-block(load-order="secondary")
    img(src="nice.gif" loading="lazy" width="300px")